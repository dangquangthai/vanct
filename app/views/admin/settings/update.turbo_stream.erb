<% if @success %>
  <%= turbo_modal_close %>

  <%= turbo_stream.replace('settings-table') do %>
    <%= render Admin::Settings::TableComponent.new(settings: @settings) %>
  <% end %>

  <%= turbo_klass "#setting_#{@setting.id}", 'selected', 'add' %>
<% else %>
  <%= turbo_modal do %>
    <%= render Admin::Settings::FormComponent.new(setting: @setting, url: admin_setting_path(@setting.id), form_method: 'PATCH', form_title: 'Cập nhật tuỳ chọn') %>
  <% end %>
<% end %>
