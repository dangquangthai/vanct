<%= turbo_modal do %>
  <%= render Admin::Users::FormComponent.new(user: @user, url: admin_tenant_user_path(@user.id, tenant_id: @tenant.id), form_method: 'PATCH', form_title: 'Cập nhật người dùng') %>
<% end %>
